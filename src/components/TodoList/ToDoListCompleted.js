import React, { useState } from 'react';
import { nanoid } from 'nanoid';

const ToDoListCompleted = ({ tasks }) => {
  console.log(tasks);

  const a = () => {
     if()
  }

  return (
    <div>
      <ul>
      </ul>
    </div>
  );
};

export default ToDoListCompleted;
